apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: moodle-lab
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://charts.bitnami.com/bitnami'
    # ¡Línea de targetRevision eliminada para forzar la última versión!
    chart: moodle
    helm:
      parameters:
        - name: moodleUsername
          value: admin
        - name: moodlePassword
          value: Admin12345*
        - name: mariadb.auth.rootPassword
          value: root12345*
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: moodle
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
